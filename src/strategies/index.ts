import { MovingAverageStrategy } from './moving-average.strategy';
import { DailySpikeStrategy } from './static/daily-spike';
import { TemplateStrategy } from './template-strategy';

export { MovingAverageStrategy, DailySpikeStrategy, TemplateStrategy };